> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Schema initialization uses simple `CREATE TABLE IF NOT EXISTS` (no migration runner framework)
- Production DB at `./data/mtgnews.db` with env var override
- Tests use `:memory:` in-memory SQLite for speed and isolation
- Signal Detail page: Summary first (inverted pyramid), then expandable transcription below
- `signals` table needs **both** `created_at` (when inserted) and `processed_at` (when LLM completes)

**Schema correction — `signals` table:**

The original spec was missing `created_at`. Issue #11 (Signal Viewer) sorts by `created_at DESC`, so this column is required. Updated schema:

| Column | Type | Notes |
|---|---|---|
| `video_id` | TEXT PK | |
| `channel_id` | TEXT FK → channels | |
| `title` | TEXT | |
| `published_at` | TEXT | YouTube published date |
| `transcription` | TEXT NOT NULL | JSON blob `{text, start, end}[]` |
| `summary` | TEXT | Nullable until LLM analysis |
| `overall_sentiment` | INTEGER 1-5 | Nullable until LLM analysis |
| `sentiment_label` | TEXT | Nullable until LLM analysis |
| `created_at` | INTEGER NOT NULL | Unix timestamp when Signal row inserted |
| `processed_at` | INTEGER | Nullable — set when LLM analysis completes |

**Label change:** `needs-triage` → `enhancement` + `ready-for-agent`